---
title: "@vencura/tools"
description: "Development tools enabled by feature flags for debugging and development purposes."
---

`@vencura/tools` provides development tools enabled by feature flags for debugging and development purposes.

## Overview

This package provides:

- Dev tools hooks for conditional feature enabling
- VConsole integration for mobile debugging
- Nuqs debug utilities
- Feature flag support

## Usage

```tsx
'use client'

import { useDevtools } from '@vencura/tools'

function App() {
  useDevtools() // Enables dev tools when feature flag is enabled

  return <div>Your app</div>
}
```

### Individual Tools

```tsx
'use client'

import { useVConsole, useNuqsDebug } from '@vencura/tools'

function DevTools() {
  useVConsole() // Enable VConsole for mobile debugging
  useNuqsDebug() // Enable Nuqs debug logging
}
```

## Feature Flags

Enable dev tools conditionally using environment variables or feature flags:

```tsx
'use client'

import { useDevtools } from '@vencura/tools'

function App() {
  const enableDevTools = process.env.NEXT_PUBLIC_ENABLE_DEV_TOOLS === 'true'

  if (enableDevTools) {
    useDevtools()
  }

  return <div>Your app</div>
}
```

## Related Documentation

See the [@vencura/tools README](../../../../packages/tools/README.md) for complete documentation.

## Related Packages

- [@vencura/ui](/docs/packages/ui) - Shared UI components (dev tools may integrate with UI components)

