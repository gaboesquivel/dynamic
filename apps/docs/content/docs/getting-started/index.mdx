---
title: "Getting Started"
description: "Quick start guide for setting up and running the Vencura monorepo, including installation, environment configuration, and development workflows."
---

Vencura is a custodial wallet platform - the Venmo of wallets. A full-stack monorepo featuring a multichain custodial wallet API, AI-powered chatbot, and Mathler game. Built with portability in mind - deploy anywhere without vendor lock-in.

## Installation

```bash
# Install dependencies
bun install
```

## Development

```bash
# Run all apps in development mode (with local Anvil blockchain)
bun run dev:local

# Run all apps in staging mode (with testnets)
bun run dev:staging

# Build all apps
bun run build
```

## Quick Start

<Diagram caption="Getting Started Flow">
  <div className="flex flex-col gap-3 p-4 text-left">
    <div className="flex items-center gap-2">
      <div className="flex h-8 w-8 items-center justify-center rounded-full bg-primary font-semibold text-primary-foreground">
        1
      </div>
      <div className="flex-1 rounded-lg border border-border bg-background p-3">
        <strong>Clone & Install:</strong> <code className="text-sm">bun install</code>
      </div>
    </div>
    <div className="ml-4 h-4 w-0.5 bg-border" />
    <div className="flex items-center gap-2">
      <div className="flex h-8 w-8 items-center justify-center rounded-full bg-primary font-semibold text-primary-foreground">
        2
      </div>
      <div className="flex-1 rounded-lg border border-border bg-background p-3">
        <strong>Configure Environment:</strong> Copy <code className="text-sm">.env-example</code> files
      </div>
    </div>
    <div className="ml-4 h-4 w-0.5 bg-border" />
    <div className="flex items-center gap-2">
      <div className="flex h-8 w-8 items-center justify-center rounded-full bg-primary font-semibold text-primary-foreground">
        3
      </div>
      <div className="flex-1 rounded-lg border border-border bg-background p-3">
        <strong>Start Development:</strong> <code className="text-sm">bun run dev:local</code>
      </div>
    </div>
    <div className="ml-4 h-4 w-0.5 bg-border" />
    <div className="flex items-center gap-2">
      <div className="flex h-8 w-8 items-center justify-center rounded-full bg-primary font-semibold text-primary-foreground">
        4
      </div>
      <div className="flex-1 rounded-lg border border-border bg-background p-3">
        <strong>Run QA:</strong> <code className="text-sm">bun run qa</code> (format, lint, build, test)
      </div>
    </div>
  </div>
</Diagram>

1. Copy example env files for each app:
   ```bash
   cp apps/api/.env-example apps/api/.env
   cp apps/web/.env-example apps/web/.env
   ```
2. Fill in your values (Dynamic environment ID, API token, etc.) in `.env` files
3. Start local development: `bun run dev:local`

## Environment Setup

This monorepo uses environment-specific configuration files. See [Environment Strategy](/docs/environment) for detailed configuration and validation patterns.

**Quick Setup:**

1. Copy example env files for each app:
   ```bash
   cp apps/api/.env-example apps/api/.env
   cp apps/web/.env-example apps/web/.env
   ```
2. Fill in your values (Dynamic environment ID, API token, etc.) in `.env` files
3. See [Environment Strategy](/docs/environment) for complete environment variable documentation

## Development Commands

<ComparisonTable
  headers={['Command', 'Description']}
  rows={[
    {
      label: 'bun run dev:local',
      values: ['Run all apps with local Anvil blockchain'],
    },
    {
      label: 'bun run dev:staging',
      values: ['Run all apps with testnet networks'],
    },
    {
      label: 'bun run build',
      values: ['Build all apps for production'],
    },
    {
      label: 'bun run format',
      values: ['Format code using Biome'],
    },
    {
      label: 'bun run lint',
      values: ['Lint code using ESLint'],
    },
    {
      label: 'bun run qa',
      values: ['Run full QA pipeline (install, format, lint, build, test)'],
    },
  ]}
  caption="Common development commands and their purposes"
/>

## Testing

```bash
# Test API (requires Foundry for Anvil local blockchain)
cd apps/api && bun run test:e2e
```

The API uses a **blackbox testing strategy** with testnet networks. See [Testing Patterns](/docs/tooling/testing-patterns) for the complete testing strategy.

## Next Steps

- Read the [Architecture](/docs/architecture) guide for monorepo structure and design principles
- Configure your [Environment](/docs/environment) with proper validation patterns
- Learn about [Deployment](/docs/deployment) and branching strategy
- Explore [Tooling](/docs/tooling) for development tools and testing patterns

